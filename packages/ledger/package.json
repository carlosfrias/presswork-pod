{
  "name": "@presswork/ledger",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "poll-receipts": "node --import tsx/esm src/poll-receipts-entry.ts",
    "daily-digest": "node --import tsx/esm src/daily-digest-entry.ts",
    "lint": "eslint src",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@presswork/shared": "*",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "vitest": "^1.0.0"
  }
}
