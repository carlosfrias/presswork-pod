// Inventory mapping was promoted to @presswork/shared so the dashboard's
// Etsy payload preview can use the same builder. Re-exported here for
// back-compat with existing publisher imports.
export { buildInventoryFromDesign, InventoryMappingError } from "@presswork/shared";
